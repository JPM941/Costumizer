<%= simple_form_for (@costume), html: { multipart: true } do |f| %>
  <%= f.input :name %>
  <%= f.input :price %>
  <%= f.input :description %>
  <%= f.input :image, as: :file %>
  <%= f.submit "Create new costume", class: "btn btn-danger" %>
<% end %>
