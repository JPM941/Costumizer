<%= simple_form_for (@costume) do |f| %>
  <%= f.input :name %>
  <%= f.input :price %>
  <%= f.input :description %>
  <%= f.input :images, as: :file, input_html: { multiple: true } %>
  <%= f.submit "Create new costume", class: "btn-primary" %>

<% end %>
